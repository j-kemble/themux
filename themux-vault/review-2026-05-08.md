# themux Vault / Project Documentation Review

> **Status:** Reference | **Last updated:** 2026-05-08

Date: 2026-05-08
Reviewer: Pip (Hermes Agent)

## Scope

Compared `/home/josh/Projects/themux/themux-vault/` against the local project scaffold in `/home/josh/Projects/themux/`.

## Fixes Applied

- Fixed the MOC's broken `[[cmuxaltprd]]` wikilink by replacing it with a valid relative Markdown link to `../cmuxaltprd.md`.
- Added repo docs links from the MOC to `docs/architecture.md`, `docs/protocol-v2.md`, and `docs/contributing.md`.
- Added a `Related` section to the MOC.
- Normalized vault note metadata so every vault note now has a `Status` and `Last updated` line.
- Added current scaffold/setup caveats to `project-structure.md` and `building-checklist.md`.
- Added a `Current Implementation Status` section to `protocol-v2.md` so the target contract is not confused with implemented code.

## Verification

- Vault Markdown notes checked: 17 (including this review)
- Broken Obsidian wikilinks after fixes: 0
- Broken relative Markdown links after fixes: 0
- Notes with `Related` sections: 17/17
- Notes with `Status` and `Last updated` metadata: 17/17
- MOC links to all vault notes: verified (no orphans)

## Important Findings

All previous "Important Findings" are now resolved:

- `.git/` exists — repo initialized at `github.com/j-kemble/themux`
- `ghostty/` is populated — vendored Ghostty source trimmed to libghostty-vt only
- `rustc`, `cargo`, `zig` are available on PATH
- `pkg-config` finds GTK4, WebKitGTK, and libadwaita via Homebrew
- CLI parses commands AND connects to the socket (system.ping/capabilities/identify)
- Socket V2 dispatch is fully wired for system.* methods
- `daemon/cmuxd-remote/` is still missing — will be needed in Phase 2

## Remaining Gaps

1. Ghostty terminal integration is wired (FFI bindings + TerminalWidget) but needs PTY plumbing to actually spawn a shell and render output
2. `cargo clippy` and `cargo fmt` haven't been run yet
3. Python integration tests need a running socket server to execute
4. `daemon/cmuxd-remote/` not yet populated (Phase 2)

## Related

- [[themux-dev]] — Map of Content
- [[project-structure]] — Repository layout and setup status
- [[building-checklist]] — Setup/build verification checklist
- [[protocol-v2]] — Target protocol and implementation status
